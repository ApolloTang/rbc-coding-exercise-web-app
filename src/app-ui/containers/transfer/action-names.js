const actionNames = [
  'transfer__init',

  `transfer__transferDefault_fetch_begin`,
  `transfer__transferDefault_fetch_success`,
  `transfer__transferDefault_fetch_fail`,

  `transfer__transfer_submit_begin`,
  `transfer__transfer_submit_success`,
  `transfer__transfer_submit_fail`
];

export default actionNames;
